# Cloudflare (Wrangler) auth for non-interactive deploys
#
# Create a token in Cloudflare Dashboard:
#   My Profile -> API Tokens -> Create Token (Custom)
#
# Then set these values as environment variables (recommended), or copy them into a local `.env`
# file in the project root (which is gitignored).
#
# Required:
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=



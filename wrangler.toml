[[d1_databases]]
"binding": "my_user_db",
"database_name": "my_user_db",
"database_id": "694057fb-479a-4672-b68a-8af5eaa7a01f"

[vars]
APP_NAME = "订阅管理"
EMAIL_FROM = "noreply@skydimo.com"
SMTP_FROM = "noreply@skydimo.com" # legacy fallback
FEEDBACK_NOTIFY_TO = "support@skydimo.com"

MOLE_API_BASE_URL = "https://api.moleapi.com/v1"
MOLE_AI_MODEL = "gpt-4o"

# Secrets (DO NOT COMMIT)
# - RESEND_API_KEY: set via `wrangler secret put RESEND_API_KEY`
# - MOLE_API_KEY: set via `wrangler secret put MOLE_API_KEY`

# Cloudflare Turnstile
# - NEXT_PUBLIC_TURNSTILE_SITE_KEY: 前端渲染所需的站点 Key（可放 vars）
# - TURNSTILE_SECRET_KEY: 后端校验所需的 Secret（建议用 `wrangler secret put` 设置，不要明文写进仓库）
NEXT_PUBLIC_TURNSTILE_SITE_KEY = "0x4AAAAAACHg5i_pQPpC0Q8a"
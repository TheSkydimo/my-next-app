# Copy to .dev.vars for local preview (Cloudflare runtime).
# Do NOT commit .dev.vars (it is ignored by .gitignore).
#
# Cloudflare Turnstile
NEXT_PUBLIC_TURNSTILE_SITE_KEY="0x4AAAAAACHg5i_pQPpC0Q8a"
TURNSTILE_SECRET_KEY=""

# Session (Remember login)
# Generate a long random secret for HMAC signing session tokens.
SESSION_SECRET=""

# Email (SMTP)
SMTP_PASS=""

# Feedback (email notify)
# - FEEDBACK_NOTIFY_TO: comma-separated recipients (e.g. "support@skydimo.com")
# - Optional: use dedicated SMTP account for feedback:
#   FEEDBACK_SMTP_HOST/FEEDBACK_SMTP_PORT/FEEDBACK_SMTP_USER/FEEDBACK_SMTP_PASS/FEEDBACK_SMTP_FROM/FEEDBACK_SMTP_ENCRYPTION
FEEDBACK_NOTIFY_TO=""

# Mole API (optional, used by /api/user/orders)
MOLE_API_KEY=""

# Load .env.development* files when running `wrangler dev`
NEXTJS_ENV=development

# Sentry (Error Monitoring)
# - Server/Edge SDK: SENTRY_DSN
# - Client SDK: NEXT_PUBLIC_SENTRY_DSN
SENTRY_DSN=""
NEXT_PUBLIC_SENTRY_DSN=""

# Log archive
# - Optional: override log prefix key in R2 (default: "logs/")
LOG_ARCHIVE_R2_PREFIX="logs/"
